# Permissions

Wattle supports only `yolo` permission mode.

`yolo` lets Wattle run requested tools without asking for runtime confirmation. This is the default and only supported behavior for both TUI and headless runs.

```bash
wattle --yolo
```
